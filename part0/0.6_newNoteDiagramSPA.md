```mermaid
sequenceDiagram
    autonumber
    participant User
    participant UI
    
    Note over User, UI: SPA Interaction
    
    User ->> UI: writes a new note and clicks "Save"
    UI ->> UI: executes the logic in Javascript (DOM-API)
    
    Note right of UI: there is no reloading
    
    UI -->> User: shows new note in JSON format
```
