```mermaid
sequenceDiagram
    participant User
    participant Server
        
    User ->> Server: goes to SPA url
    Note right of Server:no requests in Network 
    Server ->> Server:
    Note right of Server:executes the logic from the internal code in Javascript to display in html

    Note right of Server:there is only an initial load showing the DOM
    Server -->> User: shows the actual screen
```
